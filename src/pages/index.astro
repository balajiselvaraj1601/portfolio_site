---
import Layout from '@layouts/Layout.astro';
import SectionRenderer from '@components/SectionRenderer.astro';
import { homeSections, homePage, dotNavSections } from '@lib/content';

if (!homePage) throw new Error('site.json: missing home page');
---

<Layout
  path={homePage.path}
  title={homePage.seo.title}
  description={homePage.seo.description}
  dotNavSections={dotNavSections}
  singlePageViews={true}
>
  <SectionRenderer sections={homeSections} navViews={true} />
</Layout>

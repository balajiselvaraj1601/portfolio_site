---
import Section from '@components/Section.astro';
import { affiliations, site } from '@lib/content';

const title = site.sections.affiliations.title;
---

<Section id="affiliations" title={title} variant="default">
  <div class="collab-list reveal">
    {
      affiliations.items.map((a) => (
        <div class="collab-item">
          {
            a.logo ? (
              <div class="collab-logo">
                <img src={`/assets/logos/${a.logo}.svg`} alt={`${a.name} logo`} loading="lazy" />
              </div>
            ) : (
              <div class="collab-dot" aria-hidden="true"></div>
            )
          }
          <div class="collab-text">{a.name}</div>
        </div>
      ))
    }
  </div>
</Section>

<style>
  .collab-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  .collab-item {
    background: var(--bg-elev);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 1.1rem 1.25rem;
    display: flex;
    gap: 0.75rem;
    align-items: center;
  }
  .collab-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--accent-light);
    flex-shrink: 0;
  }
  .collab-logo {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 96px;
    height: 40px;
    padding: 6px 10px;
    background: #fff;
    border: 1px solid var(--border);
    border-radius: 8px;
  }
  .collab-logo img {
    display: block;
    max-width: 120px;
    max-height: 26px;
    width: auto;
    height: auto;
    object-fit: contain;
  }
  .collab-text {
    font-size: 0.85rem;
    line-height: 1.6;
    max-width: none;
  }
</style>

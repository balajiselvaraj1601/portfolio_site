---
import Section from '@components/ui/Section.astro';
import CardMark from '@components/ui/CardMark.astro';
import Icon from '@components/ui/Icon.astro';
import SectionHeading from '@components/ui/SectionHeading.astro';
import { profile } from '@lib/content';
import type { z } from 'zod';
import { contactTypeSchema } from '@schemas';

type ContactType = z.infer<typeof contactTypeSchema>;

const { contactPage } = profile;

// Every contact entry with a link (location has none → stays hidden).
const linked = profile.contact.filter((c) => c.href);

const isExternal = (href: string) => href.startsWith('http');

// Type-safe map of contact types to action labels.
const actionLabelMap: Record<ContactType, string> = {
  email: 'Send email',
  linkedin: 'View profile',
  github: 'View profile',
  kaggle: 'View profile',
  location: 'View location',
};

const actionLabel = (type: ContactType): string => actionLabelMap[type];
---

<Section
  id="contact"
  title=""
  hideTitle
  labelledBy="contact-heading"
  eyebrow={contactPage.eyebrow}
  variant="alt"
>
  <div class="contact-grid reveal">
    <!-- Left: pitch + actions -->
    <div class="contact-left">
      <SectionHeading
        title={contactPage.title}
        highlight={contactPage.titleHighlight}
        class="contact__title"
        id="contact-heading"
      />
      <p class="contact-subtitle">{profile.contactIntro}</p>

      {
        contactPage.bookingHref && (
          <div class="contact-actions">
            <a
              class="btn btn-secondary"
              href={contactPage.bookingHref}
              target="_blank"
              rel="noopener noreferrer"
            >
              <Icon name="calendar" size={18} />
              {contactPage.bookCallLabel}
            </a>
          </div>
        )
      }

      <p class="response-time">
        <Icon name="clock" size={16} />
        {contactPage.responseTime}
      </p>
    </div>

    <!-- Right: connect cards -->
    <div class="contact-right">
      <div class="connect-header">
        <span>{contactPage.connectHeading}</span>
        <span class="connect-rule" aria-hidden="true"></span>
      </div>

      <div class="connect-grid">
        {
          linked.map((c) => {
            const external = c.href ? isExternal(c.href) : false;
            return (
              <a
                class="connect-card card"
                href={c.href!}
                {...(external
                  ? { target: '_blank', rel: 'noopener noreferrer' }
                  : {})}
              >
                <CardMark icon={c.icon} iconFallback="link" />
                <h3 class="connect-name">{c.label}</h3>
                <p class="connect-val">{c.value}</p>
                <span class="connect-action">
                  {actionLabel(c.type)}
                  <Icon name="arrow-right" size={16} />
                </span>
              </a>
            );
          })
        }
      </div>
    </div>
  </div>
</Section>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--stack-xl);
    align-items: start;
  }

  /* ── Left column ──────────────────────────────────────────── */
  .contact__title {
    font-family: var(--font-display);
    font-size: var(--fs-h2);
    font-weight: 400;
    line-height: 1.1;
    color: var(--heading);
    margin-bottom: var(--stack-sm);
  }
  .contact-subtitle {
    color: var(--text-muted);
    margin-bottom: var(--stack-lg);
    font-size: var(--fs-subtitle);
    max-width: 38ch;
  }
  .contact-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--section-cta-gap);
    margin-bottom: var(--stack-md);
  }
  .response-time {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: var(--fs-md);
    color: var(--text-muted);
    margin-bottom: var(--stack-lg);
  }
  .response-time :global(svg) {
    color: var(--accent-light);
    flex-shrink: 0;
  }

  /* ── Right column ─────────────────────────────────────────── */
  .connect-header {
    display: flex;
    align-items: center;
    gap: var(--stack-sm);
    margin-bottom: 1.25rem;
    font-family: var(--font-mono);
    font-size: var(--fs-eyebrow);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text-muted);
  }
  .connect-rule {
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, var(--border-strong), transparent);
  }
  .connect-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-auto-rows: 1fr;
    gap: var(--stack-sm);
    margin-bottom: var(--stack-sm);
  }
  .connect-card {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    padding: var(--card-padding);
    text-decoration: none;
  }
  .connect-card :global(.icon-tile) {
    margin-bottom: var(--space-3);
  }
  .connect-name {
    font-size: var(--fs-card-title);
    font-weight: 600;
    color: var(--heading);
  }
  .connect-val {
    font-size: var(--fs-sm);
    color: var(--text-muted);
    margin: 0;
    max-width: none;
    flex: 1;
  }
  .connect-action {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    margin-top: 0.85rem;
    padding-top: 0.85rem;
    border-top: 1px solid var(--border);
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--accent-light);
  }
  .connect-action :global(svg) {
    transition: transform var(--dur) var(--ease);
  }
  .connect-card:hover .connect-action :global(svg) {
    transform: translateX(3px);
  }

  /* ── Responsive ───────────────────────────────────────────── */
  @media (max-width: 900px) {
    .contact-grid {
      grid-template-columns: 1fr;
      gap: var(--space-10);
    }
    .contact-subtitle {
      max-width: none;
    }
  }
  @media (max-width: 560px) {
    .connect-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

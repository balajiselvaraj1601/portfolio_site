---
import CardMark from '@components/ui/CardMark.astro';
import { collaborations, getEntityUrl, logoSrc } from '@lib/content';
---

<div class="about__collabs reveal">
  <p class="subhead-eyebrow subhead-eyebrow--muted about__collabs-heading">
    {collaborations.title}
  </p>
  <ul class="about__collabs-list" role="list">
    {
      collaborations.items.map((item) => {
        const href = getEntityUrl(item.entity);
        const src = logoSrc(item.logo);
        if (!href || !src) return null;
        return (
          <li>
            <a
              href={href}
              class="about__collab-mark"
              aria-label={item.name}
              target="_blank"
              rel="noopener noreferrer"
            >
              <CardMark
                slug={item.logo}
                logoUrl={src}
                altText=""
                useBadge={false}
                logoSize="lg"
              />
            </a>
          </li>
        );
      })
    }
  </ul>
</div>

<style>
  .about__collabs {
    margin-top: var(--stack-xl);
  }
  .about__collabs-heading {
    margin-bottom: var(--text-gap-block);
  }
  .about__collabs-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: var(--space-4);
    width: 100%;
    max-width: 100%;
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .about__collabs-list li {
    min-width: 0;
  }
  .about__collab-mark {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    min-width: 0;
    height: clamp(56px, 7vw, 80px);
    padding: var(--space-3) var(--logo-pad);
    background: var(--logo-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    text-decoration: none;
    transition:
      border-color var(--dur) var(--ease),
      box-shadow var(--dur) var(--ease),
      transform var(--dur) var(--ease);
  }
  .about__collab-mark:hover {
    border-color: var(--accent-light);
    box-shadow:
      0 0 0 1px color-mix(in srgb, var(--accent-light) 25%, transparent),
      var(--shadow-md);
  }
  @media (prefers-reduced-motion: no-preference) {
    .about__collab-mark:hover {
      transform: translateY(var(--card-lift));
    }
  }
  .about__collab-mark:focus-visible {
    outline: 2px solid var(--focus-ring);
    outline-offset: 2px;
  }
  .about__collab-mark :global(.logo-badge) {
    max-width: min(var(--logo-w-max-lg), 100%);
  }
  @media (max-width: 768px) {
    .about__collabs-list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      width: 100%;
    }
  }
  @media (min-width: 769px) and (max-width: 1024px) {
    .about__collabs-list {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      width: 90%;
    }
  }
</style>

---
import ResearchCard from '@components/cards/ResearchCard.astro';
import type { LinkList } from '@schemas';

interface Props {
  items: LinkList['items'];
  featuredFirst?: boolean;
}

const { items, featuredFirst = false } = Astro.props;
---

<div class="research-grid reveal reveal-group">
  {
    items.map((p, idx) => (
      <ResearchCard
        variant="stacked"
        featured={featuredFirst && idx === 0}
        venue={p.label}
        title={p.title}
        description={p.description}
        href={p.url}
        youtube={p.youtube}
        image={p.image}
        logo={p.logo}
        logoBadge={p.logoBadge}
      />
    ))
  }
</div>

<style>
  .research-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(var(--grid-col-min), 1fr));
    gap: var(--stack-md);
  }
</style>
